export const links = {
  home: "/",
  profile: "/profile",
  admin: "/admin",
  login: "/login",
  register: "/register",
  dashboard: "/admin/dashboard",
} as const;

export const nextApi = {
  login: "/api/login",
} as const;
